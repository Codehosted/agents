# Codehosted Agents Recorder Track

Source of truth for the static-output-to-Playwright/video recorder project. A step may only be checked after the previous step is checked and evidence is linked below it.

## Sequential checklist

- [x] 1. Project design
  - Evidence:
    - `docs/recorder-design.md` defines static parsing, flow diagrams, Playwright generation, separate video recording, narration, local timeline/reporting, and coverage/repo gates.
- [x] 2. Project screenshots
  - Evidence:
    - `docs/verification/screenshots/recorder-flow-diagram.html` renders the static output → parser → flow graph → Playwright script → recorder process → local timeline architecture with selectors/button IDs.
    - `docs/verification/screenshots/recorder-flow-diagram.png` is the captured rendered flow-diagram screenshot (verified with browser screenshot capture; `file docs/verification/screenshots/recorder-flow-diagram.png` => 1265x1011 PNG).
    - `docs/verification/screenshots/recorder-report-ui-mock.html` renders the local Mixpanel-like report UI mock with timeline events, selectors/button IDs, screenshot thumbnails, artifacts, and video playback slot.
    - `docs/verification/screenshots/recorder-report-ui-mock.png` is the captured rendered report UI screenshot (verified with browser screenshot capture; `file docs/verification/screenshots/recorder-report-ui-mock.png` => 1265x1395 PNG).
- [x] 3. Implementation
  - Evidence:
    - `package.json` defines the dependency-light Node.js package, CLI bin (`agents-recorder`), `npm test`, and `npm run coverage` scripts.
    - `bin/agents-recorder.js` implements `plan`, `generate`, `report`, and `record` CLI commands.
    - `src/static-parser.js` parses static HTML/page output into pages, links, buttons, inputs, IDs, `data-testid` values, selectors, labels, routes, and screenshot placeholders.
    - `src/flow-model.js` maps narrated story steps into visit/click/fill/assert flow steps with selectors/button IDs, assertions, screenshots, and artifacts.
    - `src/flow-diagram.js`, `src/playwright-generator.js`, `src/recorder.js`, and `src/timeline.js` generate Mermaid, Playwright, separate recorder command specs, and local Mixpanel-like timeline HTML.
    - `README.md` documents local-first/open-source philosophy and usage examples; `LICENSE` adds MIT licensing.
    - Verification: `npm test` => 6 tests passing after adding recorder command coverage and generated-video hooks.
- [x] 4. Visual verification
  - Evidence:
    - `docs/verification/sample-run/flow.json` generated by `node bin/agents-recorder.js plan --site test/fixtures/site --story test/fixtures/story.md --out docs/verification/sample-run --base-url http://localhost:4173`.
    - `docs/verification/sample-run/flow.mmd` is the generated flow diagram with `#pricing-link`, `[data-testid="demo-email"]`, `#start-recorder-demo`, `#demo-ready`, and `playback.webm`.
    - `docs/verification/sample-run/playwright.spec.js` is the generated Playwright script; line 20 preserves the dotted fill value `george@example.com`, `test.use({ video: 'on' })`, and saves `PLAYWRIGHT_VIDEO_PATH` through `video.saveAs(...)`.
    - `docs/verification/sample-run/timeline.html` is the generated local report artifact; `docs/verification/sample-run/timeline-report.png` is the browser-captured report screenshot (1280x2909 PNG) showing 5 events, selectors/button IDs, screenshot references, and a video playback slot.
    - `docs/verification/sample-run/recorder-command.json` and `node bin/agents-recorder.js record --script docs/verification/sample-run/playwright.spec.js --out docs/verification/sample-run/recording --video docs/verification/sample-run/playback.webm` document the separate recorder process, artifact-local working directory, and expected video path.
    - `PLAYWRIGHT_VIDEO_PATH=playback.webm npx playwright test playwright.spec.js --output=recording --reporter=line` (run from `docs/verification/sample-run`) passed after `python3 -m http.server 4173 --directory test/fixtures/site`; `docs/verification/sample-run/playback.webm` verified as WebM, 13,381 bytes, and native Playwright output `docs/verification/sample-run/recording/playwright-story/video.webm` was retained as recorder evidence.
    - `docs/verification/sample-run/screenshots/01-visit-home.png` through `05-assert-demo-ready.png` were captured by the generated Playwright script and verified as 1280x720 PNG files.
- [x] 5. Tests with at least 90% code coverage
  - Evidence:
    - `test/static-parser.test.js`, `test/flow-model.test.js`, `test/generators.test.js`, `test/recorder.test.js`, and `test/cli.test.js` cover parser, flow model, script/diagram/report/recorder generators, recorder process command notes, and CLI planning.
    - `npm test` => 8/8 tests passing after adding PR-feedback regressions for home-route selection, artifact-relative screenshots, and slash-safe title regexes.
    - `docs/verification/coverage-node-test.txt` captured `npm run coverage` output: all files line coverage 94.65%, function coverage 92.39%, `src/playwright-generator.js` and `src/recorder.js` line/function coverage 100%, 8/8 tests passing.
- [x] 6. Push to GitHub in a new Codehosted/agents project
  - Evidence:
    - Created public GitHub repo: https://github.com/Codehosted/agents (`gh repo create Codehosted/agents --public --description "Local-first static-output to Playwright recorder and timeline toolkit" --source=. --remote=origin`).
    - Pushed seed `main` branch and implementation branch `feat/recorder-scaffold` with `git push -u origin main` and `git push -u origin feat/recorder-scaffold`.
    - Opened draft PR: https://github.com/Codehosted/agents/pull/1 (`feat: scaffold recorder automation toolkit`) from `feat/recorder-scaffold` to `main`.
    - `gh pr view --json number,url,title,state,isDraft,headRefName,baseRefName` confirmed PR #1 is open and draft.
    - `gh pr checks 1 --watch --interval 10` reported no checks configured on the branch.

## Product acceptance notes

- Open-source-friendly local CLI/library for turning static page output/user stories into a flow diagram and Playwright script.
- Generated flow includes page URLs, button/link IDs/selectors, actions, assertions, and screenshots.
- Separate screen recorder process can capture automation playback to video.
- Supports narration/script text so a user can describe what to do and watch playback afterward.
- Prefer faster static output parsing for planning, then Playwright only for execution/recording.
- Does not require proprietary analytics tools; Mixpanel-like event timeline/report is generated locally.
